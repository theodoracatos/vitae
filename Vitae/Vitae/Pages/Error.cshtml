@page  "{StatusCode?}"
@model ErrorModel
@{
    ViewData["Title"] = "Error2";
}

<div class="container h-100">
    <div class="row align-items-center h-100">
        <div class="col-6 mx-auto">
            <div class="jumbotron">
                <form method="post">
                    <h4><i class="fa fa-heart-broken fa-fw"></i>@Model.Code - @Model.Message</h4>
                    <hr />
                    <div class="form-row">
                        <div class="form-group col">
                            <p class="text-danger">
                                @Model.Message
                                <br />
                                <code>@Model.Method @Model.Origin</code>
                            </p>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col mb-0 text-right">
                            <a id="forgot-password" class="btn btn-outline-danger" href="javascript:history.back()">@SharedResource.Back</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>